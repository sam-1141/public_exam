import React from 'react';
import Layout from '../../../layouts/Layout';

const SingleExamLeaderboard = () => {
    return (
        <div>
            single Exam Leaderboard
        </div>
    );
};

SingleExamLeaderboard.layout = (page) => <Layout>{page}</Layout>
export default SingleExamLeaderboard;